<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Card view -->
    <style name="square_card_view" parent="@style/Widget.MaterialComponents.CardView">
        <item name="shapeAppearanceOverlay">@style/ShapeAppearanceOverlay.MaterialCardView.Cut</item>
    </style>

    <style name="ShapeAppearanceOverlay.MaterialCardView.Cut" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSizeTopRight">0dp</item>
        <item name="cornerSizeTopLeft">0dp</item>
        <item name="cornerSizeBottomRight">0dp</item>
        <item name="cornerSizeBottomLeft">0dp</item>
    </style>

    <!-- Squared FAB -->
    <style name="fab_square" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">0dp</item>
    </style>

    <!-- Calendar view
    <style name="calendar_view" parent="@android:style/Theme">
        <item name="android:calendarViewStyle">@style/Widget.CalendarView.Custom</item>
    </style>

    <style name="Widget.CalendarView.Custom" parent="android:Widget.CalendarView">
        <item name="android:focusedMonthDateColor">@color/black</item>
        <item name="android:weekNumberColor">@color/black</item>
        <item name="android:selectedWeekBackgroundColor">@color/white</item>
        <item name="android:selectedDateVerticalBar">@color/black</item>
        <item name="android:unfocusedMonthDateColor">@color/black</item>
        <item name="android:weekDayTextAppearance">@style/TextAppearance.AppCompat.Small</item>
        <item name="android:dateTextAppearance">@style/TextAppearance.AppCompat.Small</item>
    </style> -->
</resources>